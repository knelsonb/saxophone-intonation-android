# BellCurve — Test-Run Ledger

One row per run. **Tier:** `A/off`=off-device logic, `A/AVD`=emulator, `B`=real Pixel HW.
**V:** GREEN / RED / PARTIAL / BLOCKED. Honesty: only what was DEMONSTRATED (per the test plan §0).

| Run | Tier | Suite | V | Notes |
|---|---|---|---|---|
| 2026-05-28 | A/off | regression (tsc+legacy+jest) | GREEN | tsc clean; yin pitch-math + legacy + renderCount pass (2 todo). Baseline @5405ea0. |
| 2026-05-28 | B | #65 bob-slide | GREEN | on-device: bob slides upper-third@100 → pivot@240 BPM (captures 12/17). |
| 2026-05-28 | B | #68 120Hz metro pin | PARTIAL | pin+release work (HUD 120 running / 48 stopped) but the LTPO **demotes to 60Hz at low BPM** — Window hint isn't a hard pin; needs Surface.setFrameRate. |
| 2026-05-28 | B | #64 gate-1 sweep | BLOCKED | egress VALIDATED (ShadowBeat/PendDiag/gate1 streaming; ramp visible @30 BPM, vf/vs flags the 60Hz demotion) but the full bookended sweep **WiFi-dropped** (0 lines captured). Needs a stable Pixel link (USB) + a clean 120Hz hold. |
| 2026-05-28 | B | #67 tuner overdraw | PENDING | fix committed 2bcd5db; on-device needle-float + LedRow-truncation check needs a sounding note (fold with A4 loopback). |
| 2026-05-28 | B | A4 loopback | PENDING | driver ready (a4_loopback.sh); needs acoustic setup + a stable link. |
| 2026-05-28 | A/off | regression | GREEN | tsc+legacy+jest clean |
| 2026-05-28 | A/AVD | install | GREEN | apk installed |
| 2026-05-28 | A/AVD | launch-no-crash | GREEN | no crash on launch |
| 2026-05-28 | A/AVD | stability(meminfo) | INFO | see meminfo-start/30s.txt (compare TOTAL PSS for growth) |
| 2026-05-28 | A/AVD | layout-capture | ISSUE | (1) LANDSCAPE TUNER breaks — strobe overdraws the toggle + DRONE clipped; REAL (app.json orientation="default" allows landscape) → task #69. (2) Portrait: AVD swiftshader mis-renders the strobe (the real Pixel renders it fine) ⟹ headless-GL AVD is UNRELIABLE for GL-component layout review — use the Pixel, or try `-gpu swangle_indirect`/`-gpu host`. AVD stays valid for regression/crash/functional/stability. |
| 2026-05-28 | A/AVD | monkey-fuzz | GREEN | 2000 random events @100ms, NO crash (crash buffer empty). App survives heavy fuzzing. |
| 2026-05-28 | A/AVD | CentArc layout | ISSUE | DEFAULT tuner style (arc) renders OVERLAPPING the TUNER/CUSTOMIZATION toggle + gap below, on pixel_7 1080×2400. RN-layout (swiftshader-independent) → real. Pixel was on STROBE style so this arc bug was never seen there — may be the operator's actual #67 "floating tuner". Investigating CentArc/tunerWidgets. |
| 2026-05-28 | A/AVD matrix | TUNER layout | ISSUE | #69+#70 REPRODUCE across 3 configs: bc_test pixel_7 portrait (arc overlaps toggle), phone landscape (break), bc_tablet 2560×1600 landscape (arc overlaps toggle + +00¢/TAP-LOG/COLLECT jumble). ⟹ REAL responsive-layout bug in TunerScreen (centerPortrait centering + arc stack overflow), NOT config-specific/swiftshader. Fix-diagnosis sub-agent dispatched. |
| 2026-05-28 | A/off | audit: accuracy (pitch/cents/A4) | GREEN | sauron: 0 crit/warn. YIN + cents + transposition + drone/pipes pitchBend traced mic→display→disk→synth, verified by hand. Only cosmetic: 0.5¢ tier raw toFixed(1) in 2/3 widgets (→#13). |
| 2026-05-28 | A/off | audit: hardening (crash/edge) | GREEN | uruk-hai: CLEAN. Consistent clamping (A4/BPM/MIDI/vel), NaN/Inf guards, div-by-0 guards, lifecycle teardown; silence-over-wrong upheld. |
| 2026-05-28 | A/off | audit: perf/leaks/anim | INFO | legolas: NO unbounded leaks. 7 warn (RAF tick runs 120Hz off-tuner-tab; un-debounced prefs setters; bus Array.from per MIDI event; runOnJS re-arm on BPM scrub) + 8 note → #12. |
| 2026-05-28 | A/off | audit: UX/states/a11y | ISSUE | frodo: 1 CRITICAL — metro profiles never persist, lost on restart (#10) — +5 warn (DRONE-ON lies before pitch lock; TUNER CLEAR no confirm/undo; stepper no limit feedback; no haptics on TAP/START/RECORD) + 4 note → #10/#11. |
| 2026-05-28 | A/off | audit: style/dead-code | INFO | gollum: 5 nits — dead arcNeedleTip; 2 stray console.warn; unused freqToMidi export → #13. |
| 2026-05-28 | A/AVD | #70 fix (Edit 1) | GREEN | VERIFIED bc_test pixel_7: built release APK (JS re-bundled — createBundleReleaseJsAndAssets ran), LIVE-mode CentArc now renders cleanly BELOW the TUNER/CUSTOMIZATION toggle — overlap GONE, arc top-anchored (avd-tuner-70fix-LIVE.png). COLLECT-mode bucket card also clean (avd-tuner-70fix-portrait.png). |
| 2026-05-28 | A/AVD | metro persistence (gandalf) | PARTIAL | MetroScreen renders all 4 User profiles post-refactor, NO crash (avd-metro-postfix.png); tsc+jest GREEN. NOT YET demonstrated: edit-survives-restart (blind-tap UI drive unreliable on AVD → defer to Pixel/instrumented). Impl: profiles lifted to useMetronome, persisted via prefsUpdate(metroProfilesJson), legacy-safe fallback. |
| 2026-05-28 | A/AVD | monkey-smoke (post-batch) | GREEN | 800 events @80ms across all screens incl. refactored METRO — crash buffer empty, no FATAL. |
| 2026-05-28 | A/off | batch tsc+jest | GREEN | tsc_rc=0; legacy yin pitch-math + renderCount pass. Batch = #70 layout + 5 polish nits + metro-persistence + test mock. |
| 2026-05-28 | A/AVD | #11 drone-waiting (Fix 1) | GREEN | VERIFIED bc_test: toggling DRONE with no pitch locked now shows amber "DRONE ON · WAITING FOR PITCH" + expands VOL/SEMI (was falsely "DRONE ON / following pitch" while silent — a silence-over-wrong violation). Emulator mic-less = ideal test (pitch never locks). avd-drone-waiting.png. tsc+jest green; bundle PITCH confirmed (byte-mode). |
| 2026-05-28 | A/AVD | #11 CLEAR arm-to-confirm (Fix 2) | PARTIAL | Implemented + tsc+jest green; 'TAP AGAIN' in bundle; tap-twice idiom mirrors IntonationTable (4s disarm, focus/unmount/bucket-gone cleanups). Arm-behavior NOT AVD-demonstrated — CLEAR is correctly disabled w/o an active bucket (needs a sounding note) → defer to note-input/Pixel. |
| 2026-05-28 | A/AVD | #11 stepper limit-dim (Fix 3) | GREEN | VERIFIED bc_test: floored drone SEMI to +12 → the + arrow dims (opacity 0.3) + disabled while − stays bright; VOL @50% both bright. Uniform lowCutStepDisabled across all 10 steppers/20 arrows (SetupScreen night-darken/warmth/low-cut/A4/click-offset/click-vol + TunerScreen drone VOL/SEMI ×2). ±1e-6 tol on float steppers (warmth 0.2×5 < 1.0 in IEEE-754); touch targets unchanged; tsc+jest green. avd-stepper-limit.png. |
| 2026-05-28 | A/AVD matrix | #70 on bc_small (tightest) | GREEN | VERIFIED pixel_2 1080×1920@420 = 411×731dp (tightest profile): LIVE CentArc renders cleanly below the toggle — overlap FIXED here too (now confirmed pixel_7 914dp + pixel_2 731dp). No-note state fits with margin (avd-small-tuner-default.png). INFERRED CAVEAT (not demoable on mic-less AVD): with a real note the ~202dp NoteReadout block (font 130) likely exceeds the ~227dp centerPortrait budget on 731dp → bottom hz/cents-detail would clip (big note+cents still show). → Edit 2 (density-aware noteFontSize) + Pixel with-note verify = task #16. |
| 2026-05-28 | A/AVD | #12 SAFE-tier perf | GREEN | SHIPPED: 10 prefs setters → debounced prefsUpdate (keys verified: peakLock/theme/nightDarken/nightWarmth/tunerStyle/metroStyle/deckStyle/metroClickOffsetMs/metroOutputRoute/lowCutDb; clamps preserved; setA4Hz/setHiFiMode untouched) + PCM-zero early-break + yinPitch IIFE-lift + winnerFreqs sort-in-place + VuDisplay setTick removal. tsc+jest green; monkey 800 no-crash (exercised SETUP/DECK/tuner). ZERO behavioral change — perf gain NOT AVD-measurable (not claimed). MEDIUM (bucket-skip) + FRAGILE (bus-hotpath/RAF-gate) deferred per legolas tiers. |
| 2026-05-28-tierA | A/off | regression | GREEN | tsc+legacy+jest clean |
| 2026-05-28-tierA | A/AVD | install | GREEN | apk installed |
| 2026-05-28-tierA | A/AVD | launch-no-crash | GREEN | no crash on launch |
| 2026-05-28-tierA | A/AVD | stability(meminfo) | INFO | see meminfo-start/30s.txt (compare TOTAL PSS for growth) |
| 2026-05-28-tierA | A/AVD | layout-capture | INFO | avd-portrait/landscape.png — manual layout review (#67/tab-fit) |
| 2026-05-28 | A/off | midiBus gating tests | GREEN | treebeard +2 regression tests (17 assertions, all pass): emit listener-snapshot under unsubscribe-during-callback (N=3 + single) + resolveRole across reserve→fire→release→re-reserve + unreserved→drone fallback. GATES the deferred #12 FRAGILE bus-hotpath opt (size===1 fast-path + reverse channel→role map) → now shippable-later w/ behavior locked. Hardens the #64-critical bus (was under-tested). Note: createMidiBus fire-handler uses JS Set-iteration (no snapshot); createMidiBusCore.emit does snapshot — both gated. |
| 2026-05-28 | A/AVD | #12 bus hot-path opt | GREEN | SHIPPED (gated by Test 13/14): emit size===1 fast-path (skip Array.from snapshot for the common single-listener case) + resolveRole O(1) reverse channel→role map (mirrored at the ONLY 2 reservations mutation sites — sauron-audited exhaustively, no desync path; dispose drains via release()). Behavior-preserving (Test 13/14 + all legacy pass); tsc clean; diff reviewed; monkey 800 no-crash (exercised metro/drone/tuner bus paths on-device incl. native commandFired→resolveRole). #64-critical path. |
| 2026-05-29 | A/off | yin pitch sweep + chars | GREEN | Extended yin.test.ts: A2..C6 sweep @44.1k+48k (cents logged) + edges (A1 −0.8c good; C7/C8 octave-error near period-floor) + 5-partial harmonic. FINDING: yinPitch single-frame parabolic-interp bias up to ±29c, PITCH-DEPENDENT (A4 +7.8, C5 +11.5, A5@48k −29), PERSISTS on harmonic A3 (+7.9c = pure A3) → grid-alignment artifact, NOT sine ambiguity; at the engine's REAL 4096 window (RING_BUFFER_CAPACITY); masked before by ±3Hz test tolerances. NOT a proven tuner defect — real dithering tones + engine median-voting (line 1785) likely average out the single-frame bias; UNQUANTIFIED → pitch-via-mic HW verify (#17). Tests now assert non-null + conf<0.10 + ±60c octave-guard; cents LOGGED as the metric. |
| 2026-05-29 | A/off | wave regression review | CLEAN | uruk-hai adversarial review of the FULL wave 6c3da27..HEAD (10 commits): NO confirmed bugs. Deep-checked metro-persistence (profile round-trip, custom time-sig num=pattern.length/den→4 is designed U22 behavior, activeSlot 0↔1-based conversion, StrictMode double-invoke idempotent), prefs-debounce (coalesce/merge/retry-race all safe), bus-opt (reserve/release channelToRole sync), layout/UX (centerPortrait anchor/clip, clearArmed tap-twice + focus/unmount/bucket-vanish cleanup, stepper bounds no off-by-one, droneWaiting, winnerFreqs in-place sort safe, maxAbs early-break correct). Wave verified solid. |
| 2026-05-29 | A/AVD matrix | shipped fixes @360dp | GREEN+nit | Simulated 360×800dp (bc_test wm density 480): #70 CentArc HOLDS (no overlap — top-anchor robust at narrow+short); #11 drone-waiting pill 'DRONE ON · WAITING FOR PITCH' FITS one line (frodo's 360dp-wrap concern RESOLVED — font 10/1.2); #11 stepper-dim holds (SEMI + dimmed @+12). MINOR NIT → #19: TUNER header row overflows at 360dp — instrument pill squeezed ~empty (can't see instrument) + 'CONCERT'→'CONCI' truncated + TAP-TO-LOG ellipsizes. (Pixel 9 Pro 411dp unaffected.) avd-360dp-tuner.png / -drone-waiting.png. Density reverted. |
| 2026-05-29 | A/AVD | #19 header fix | GREEN | frodo width-gated (width<390) graceful degradation in TopBar (tunerWidgets.tsx): badge minWidth:40+ellipsis, narrow-trim stepper/toggle padding+letterspacing (step-btn 48→36 visual but 48dp touch via hitSlop), CONCERT numberOfLines={1}. VERIFIED bc_test: @360dp (density 480) badge 'BB …' (was empty) + CONCERT FULL (was 'CONCI'); @411dp (normal) NO-OP (badge 'B…' + CONCERT full, identical to pre-fix). tsc+jest green. NOTE: 411dp badge 'B…' truncation is PRE-EXISTING + AVD-font-dependent (real Pixel may differ) — not a regression, not flagged. avd-360dp/411dp-header-fix.png. |
| 2026-05-29 | A/off+build | #67 LedRow end-LED clip | GREEN(math+build) | FIX: LedRowDisplay root maxWidth now folds in the 8+8 paddingHorizontal (+16) → the bare LED row (11 LEDs + 10 gaps) fits its content box EXACTLY; end LEDs + ±25¢ legend no longer spill/clip; overflow:'hidden' is now belt-and-braces. Math-provable (content box = bare row width); tsc+jest green; release build clean (bundles). LED-style VISUAL confirm NOT achieved: the CUSTOMIZATION style-picker needs precise small-button taps + coord estimation from downscaled screencaps is imprecise (repeated mis-taps; nudged test-AVD A4 to 438 — harmless). Low-risk layout constant; visual-confirm = trivial follow-up. LESSON: AVD small-button nav is unreliable (adb-auto selector can't read the RN tree); reserve coord-taps for large/reference-captured targets. |
| 2026-05-29 | A/off | #17 yin-window experiment | INFO | Tested whether a larger YIN window reduces the ±29c bias: yinPitch CAPS at N=4096 (fixed scratch buffers, yin.ts:88-90 → null for N>4096). Window-bump is NOT a drop-in fix (needs buffer resize + more audio-thread CPU/latency; wouldn't fix the LOCAL parabolic-interp error). ⟹ #17's real fix is the sub-sample interpolator (core change, Pixel-verify-first); window-bump path RULED OUT. (throwaway experiment, deleted.) |
| 2026-05-29 | A/AVD | #20 UX audit-notes | GREEN(code)+smoke | frodo fixed the 3 NOTE-severity UX gaps (warnings shipped in #11): (1) synth-unavailable chip → 'restart the app' next-step + re-anchored top-centred off the DECK/TUNER controls (App.tsx); (2) DECK failed-take → persistent inline error band + 'CLEAR & RE-RECORD' CTA, cleared at disposePlayer chokepoint (useDeck+DeckScreen); (3) profile-name empty-blur → reverts to saved name + 'Name can't be empty' hint (ProfileEditorAccordion) — #10 round-trip double-defended in useMetronome (empty→UserN on read+write, untouched — verified safe). tsc+jest+legacy green; monkey 800 no-crash (normal render). Conditional error/transient states (synth-fail, take-load-fail) NOT AVD-demonstrable (rare) → code/review-gated, visual deferred to Pixel. |
